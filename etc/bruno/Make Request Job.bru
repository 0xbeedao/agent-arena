meta {
  name: Make Request Job
  type: http
  seq: 30
}

post {
  url: {{host}}/api/debug/request
  body: json
  auth: inherit
}

body:json {
  {
    "url": "{{host}}/api/responders/health/$JOB$",
    "method": "GET",
    "delay": 0,
    "data": ""
  }
  
}
