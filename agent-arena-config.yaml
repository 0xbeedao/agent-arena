arena:
  db:
    filename: <projectroot>/arena.db
  logging:
    level: DEBUG
    capture: False
    loggers:
      factory: DEBUG
      service: DEBUG
      controller: DEBUG
  url: http://localhost:8000
  workers: 4

actor:
  db:
    filename: <projectroot>/actor.db
  logging:
    level: DEBUG
    capture: False
    loggers:
      factory: DEBUG
      service: DEBUG
      controller: DEBUG
  url: http://localhost:8001

messagebroker:
  url: nats://localhost:4443

scheduler:
  db:
    filename: <projectroot>/arena.db
  delay: 1
  logging:
    level: DEBUG
    capture: False
    loggers:
      factory: DEBUG
      service: DEBUG
      controller: DEBUG
  max_concurrent: 5
  url: http://localhost:8080

uuid:
  wordlist: <projectroot>/etc/words.csv

prod:
  better_exceptions: False

dev:
  better_exceptions: True
